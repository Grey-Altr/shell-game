const cups = [imgOne, imgTwo, imgThree]

function getRandomItem(arr) {
    const randomCupNumber = Math.floor(Math.random() * 3);
    return arr[randomCupNumber]
}

function cupGameLogic {
    resetImages();
    total++;
    const winningCup = getRandomItem(cups)
    
}
